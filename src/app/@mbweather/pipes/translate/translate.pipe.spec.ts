import { MBWTranslatePipe } from './translate.pipe';

describe('TranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new MBWTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
